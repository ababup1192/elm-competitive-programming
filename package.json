{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "exec": "npm run build && node dist/bundle.js < input",
    "submit": "npm run build && cat dist/bundle.js | pbcopy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm-hot": "^1.1.6",
    "esbuild": "^0.11.2",
    "esbuild-node-tsc": "^1.3.0",
    "esbuild-plugin-elm": "0.0.2",
    "fs": "0.0.1-security",
    "node-elm-compiler": "^5.0.5"
  }
}